cxx = g++
ldflags = -lxcb -lxcb-util -lxcb-icccm -lpthread
cxxflags = -std=c++14 -O3 -g

: foreach *.cpp |> $(cxx) $(cxxflags) -c -Wall %f -o %o |> %B.o {objs}
: {objs} |> $(cxx) $(ldflags) $(cxxflags) %f -o %o |> controllemonbar
